name: Build Agent Installers

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  workflow_dispatch:

jobs:
  # ══════════════════════════════════════════════════
  # Windows: PyInstaller → Inno Setup installer .exe
  # ══════════════════════════════════════════════════
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python deps
        run: pip install pyinstaller psutil requests pystray Pillow

      - name: Build .exe (no console)
        working-directory: agent
        run: |
          pyinstaller --onefile --windowed `
            --name "NetMonitor-Agent" `
            --hidden-import pystray._win32 `
            --hidden-import PIL.ImageFont `
            --hidden-import PIL.ImageDraw `
            --hidden-import winreg `
            --hidden-import tkinter `
            --hidden-import tkinter.simpledialog `
            agent.py

      - name: Install Inno Setup
        run: choco install innosetup --no-progress -y

      - name: Download Chinese Inno Setup language file
        working-directory: agent
        run: |
          $dir = "C:\Program Files (x86)\Inno Setup 6\Languages"
          $url = "https://raw.githubusercontent.com/jrsoftware/issrc/main/Files/Languages/Unofficial/ChineseSimplified.isl"
          Invoke-WebRequest -Uri $url -OutFile "$dir\ChineseSimplified.isl"

      - name: Compile installer
        working-directory: agent
        run: |
          New-Item -ItemType Directory -Force -Path installer_out | Out-Null
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

      - uses: actions/upload-artifact@v4
        with:
          name: NetMonitor-Agent-Windows-Setup
          path: agent/installer_out/NetMonitor-Agent-Setup.exe

  # ══════════════════════════════════════════════════
  # macOS Intel (x86_64) — macos-13 runner
  # ══════════════════════════════════════════════════
  build-macos-intel:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: pip install pyinstaller psutil requests pystray Pillow

      - name: Build Intel .app
        working-directory: agent
        run: |
          pyinstaller --windowed \
            --name "NetMonitor Agent" \
            --hidden-import pystray._darwin \
            --hidden-import PIL.ImageFont \
            --hidden-import PIL.ImageDraw \
            --hidden-import tkinter \
            --hidden-import tkinter.simpledialog \
            agent.py

      - name: Create Intel DMG
        working-directory: agent
        run: |
          brew install create-dmg
          create-dmg \
            --volname "NetMonitor Agent" \
            --window-size 560 340 \
            --icon-size 128 \
            --icon "NetMonitor Agent.app" 140 150 \
            --hide-extension "NetMonitor Agent.app" \
            --app-drop-link 420 150 \
            --no-internet-enable \
            "NetMonitor-Agent-Mac-Intel.dmg" "dist/" || \
          (cd dist && zip -r ../NetMonitor-Agent-Mac-Intel.zip "NetMonitor Agent.app")

      - uses: actions/upload-artifact@v4
        with:
          name: NetMonitor-macOS-Intel
          path: |
            agent/NetMonitor-Agent-Mac-Intel.dmg
            agent/NetMonitor-Agent-Mac-Intel.zip

  # ══════════════════════════════════════════════════
  # macOS Apple Silicon (arm64) — macos-latest runner
  # ══════════════════════════════════════════════════
  build-macos-arm:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install deps
        run: pip install pyinstaller psutil requests pystray Pillow

      - name: Build ARM64 .app
        working-directory: agent
        run: |
          pyinstaller --windowed \
            --name "NetMonitor Agent" \
            --hidden-import pystray._darwin \
            --hidden-import PIL.ImageFont \
            --hidden-import PIL.ImageDraw \
            --hidden-import tkinter \
            --hidden-import tkinter.simpledialog \
            agent.py

      - name: Create ARM64 DMG
        working-directory: agent
        run: |
          brew install create-dmg
          create-dmg \
            --volname "NetMonitor Agent" \
            --window-size 560 340 \
            --icon-size 128 \
            --icon "NetMonitor Agent.app" 140 150 \
            --hide-extension "NetMonitor Agent.app" \
            --app-drop-link 420 150 \
            --no-internet-enable \
            "NetMonitor-Agent-Mac-AppleSilicon.dmg" "dist/" || \
          (cd dist && zip -r ../NetMonitor-Agent-Mac-AppleSilicon.zip "NetMonitor Agent.app")

      - uses: actions/upload-artifact@v4
        with:
          name: NetMonitor-macOS-AppleSilicon
          path: |
            agent/NetMonitor-Agent-Mac-AppleSilicon.dmg
            agent/NetMonitor-Agent-Mac-AppleSilicon.zip

  # ══════════════════════════════════════════════════
  # GitHub Release（tag 触发）
  # ══════════════════════════════════════════════════
  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [ build-windows, build-macos-intel, build-macos-arm ]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with: { name: NetMonitor-Agent-Windows-Setup, path: release/ }
      - uses: actions/download-artifact@v4
        with: { name: NetMonitor-macOS-Intel, path: release/ }
      - uses: actions/download-artifact@v4
        with: { name: NetMonitor-macOS-AppleSilicon, path: release/ }

      - uses: softprops/action-gh-release@v2
        with:
          files: |
            release/NetMonitor-Agent-Setup.exe
            release/NetMonitor-Agent-Mac-Intel.dmg
            release/NetMonitor-Agent-Mac-Intel.zip
            release/NetMonitor-Agent-Mac-AppleSilicon.dmg
            release/NetMonitor-Agent-Mac-AppleSilicon.zip
          body: |
            ## NetMonitor Agent ${{ github.ref_name }}

            | 平台 | 下载 | 适用 |
            |------|------|------|
            | 🪟 Windows | `NetMonitor-Agent-Setup.exe` | 64位 |
            | 🍎 macOS | `NetMonitor-Agent-Mac-AppleSilicon.dmg` | M1/M2/M3/M4 |
            | 🍎 macOS | `NetMonitor-Agent-Mac-Intel.dmg` | Intel Mac |

            **Windows：** 双击安装包 → 向导完成后自动启动，出现在系统托盘
            **macOS：** 打开 DMG → 拖拽到 Applications → 右键打开（首次绕过 Gatekeeper）
          generate_release_notes: true
